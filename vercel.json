{
  "buildCommand": "cd cbt-app && npm install && npm run build",
  "outputDirectory": "cbt-app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
